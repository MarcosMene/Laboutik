{% extends 'base.html.twig' %}

{% block title %}
  Mot de passe oublié - LabouTik -
{% endblock %}

{% block content %}
  <div class="block-small">
    <h1 class="h3 mb-3 font-weight-normal">Réinitialiser votre mot de passe</h1>
    {% for message in app.flashes('notice') %}
      <div>{{ message|raw }}</div>
    {% endfor %}
    {{ form(form) }}
  </div>
{% endblock %}
